<Project Sdk="Microsoft.NET.Sdk">

  <!--
    UI 非依存。ここが本体（設計 §4）。
    Avalonia への参照を足さないこと。足した瞬間に「UI に業務を書かない」が崩れる。
  -->

  <PropertyGroup>
    <RootNamespace>MultiAIAgentCompany.Core</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- **スクリプトの組み立てと窓 id の読み取りをテストで固定する**（設計 §32）。
         公開 API にはしない —— あれは実装の細部で、外から呼ぶものではない。 -->
    <InternalsVisibleTo Include="MultiAIAgentCompany.Tests" />
  </ItemGroup>

</Project>
